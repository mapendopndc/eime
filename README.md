# Glulam Beam Designer
Performs code checks against glulam beams from an etabs frame forces export

Uses the EIME library for its engineering formula module.
Uses streamlit to generate interactive dashboard.